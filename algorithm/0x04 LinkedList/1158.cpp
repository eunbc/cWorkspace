#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n,k;
    cin >> n >> k;

    int pre[5001];
    int nxt[5001];
    vector<int> v;

    for(int i=1;i<=n;i++) {
        pre[i]
    }
    
        
}