#include <bits/stdc++.h>
using namespace std;

int N;
bool board[101][101];

int main(void){ 
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> N;
    while(N--) {
        int x,y,d,g;
        cin >> x >> y >> d >> g;
        board[y][x] = 1;

        while(g--) {
            
        }
    }


    for(int i=0; i<100; i++) {
        for(int j=0; j<100 ;j++) {
            if(!board[i][j]) continue;
            
        }
    }

    


}