#include <bits/stdc++.h>
using namespace std;

string solution(string s) {
    for(int i=0; i<s.size(); i++) {
        cout << s[i] << ' ';
    }
    string answer = "";
    return answer;
}

int main(void){ 
    ios::sync_with_stdio(0);
    cin.tie(0);
    string str = "3people unFollowed me";
    cout << solution(str);    
}